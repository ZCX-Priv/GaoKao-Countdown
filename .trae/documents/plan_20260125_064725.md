## Chrome 61+ 浏览器兼容性适配计划

### 检测到的兼容性问题

#### 1. card.css
| 行号 | 问题代码 | 原因 | 修复方案 |
|------|----------|------|----------|
| 33 | `mask-composite: exclude;` | Chrome 88+ 支持 | 删除该行（已有 `-webkit-mask-composite: xor`） |
| 63 | `gap: 35px;` | Flexbox gap 需要 Chrome 84+ | 移除 gap，改为子元素 `margin` |
| 187 | `gap: 15px;` | 同上 | 同上（媒体查询内） |

#### 2. settings.css
| 行号 | 问题代码 | 原因 | 修复方案 |
|------|----------|------|----------|
| 35,48,56,74,82,98 | `clip-path: inset()` | Chrome 83+ 支持 | 保留，动画在旧版降级为直接显示 |
| 130 | `gap: 10px;` | Flexbox gap 需要 Chrome 84+ | 移除 gap，改为子元素 `margin` |
| 277 | `gap: 10px;` | 同上 | 同上 |
| 329 | `gap: 8px;` | 同上 | 同上 |
| 337 | `gap: 4px;` | 同上 | 同上 |

#### 3. notice.css
| 行号 | 问题代码 | 原因 | 修复方案 |
|------|----------|------|----------|
| 9 | `gap: 10px;` | Flexbox gap 需要 Chrome 84+ | 移除 gap，改为子元素 `margin` |

#### 4. liquid-glass/liquid-glass.css
| 行号 | 问题代码 | 原因 | 修复方案 |
|------|----------|------|----------|
| 27,37 | `inset: 0;` | Chrome 87+ 支持 | 替换为 `top:0; right:0; bottom:0; left:0;` |
| 5,29,39 | `56.00000000000001px` | 浮点数精度冗余 | 简化为 `56px` |

### 垃圾代码清理

| 文件 | 位置 | 内容 | 原因 |
|------|------|------|------|
| card.css | 44行 | `/* Overlay handles the visual effect now */` | 冗余注释 |
| card.css | 47行 | `/* Pseudo-element removed as effect is now global */` | 冗余注释 |
| settings.css | 87行 | `/* Liquid Mode for Modal */` | 冗余注释 |
| settings.css | 92行 | `/* Overlay handles the visual effect */` | 冗余注释 |

### 修改文件清单

| 文件 | 修改项数 | 主要修改 |
|------|----------|----------|
| styles/card.css | 4 | 删除 `mask-composite`，移除两处 `gap`，清理注释 |
| styles/settings.css | 5 | 移除四处 `gap` |
| styles/notice.css | 1 | 移除 `gap` |
| liquid-glass/liquid-glass.css | 5 | 替换 `inset`，简化 border-radius，清理注释 |

### JavaScript
无需修改，所有 API 均兼容 Chrome 61+