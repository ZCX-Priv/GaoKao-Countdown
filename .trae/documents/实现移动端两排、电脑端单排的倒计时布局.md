修改 `styles/card.css` 文件，实现响应式布局：

**移动端（≤768px）**：

* 第一排：天（单独居中显示，大字体）

* 第二排：时、分、秒（横向排列，中等字体）

**电脑端（>768px）**：

* 保持单排横向布局（天、时、分、秒）

具体修改：

1. 更新媒体查询断点为 768px
2. `.countdown-display` 改为 column 布局
3. 第一个 `.time-unit`（天）单独一行
4. 其余 `.time-unit` 在第二排横向排列
5. 调整字体大小以适应新布局

